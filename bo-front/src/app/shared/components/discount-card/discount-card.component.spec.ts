import { DiscountCardComponent } from './discount-card.component';

describe('DiscountCardComponent', () => {
  it('should create', () => {
    const comp = new DiscountCardComponent();
    expect(comp).toBeTruthy();
  });
});
