import { ConcatenateSrcDirective } from './concatenate-src.directive';

describe('ConcatenateSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new ConcatenateSrcDirective();
    expect(directive).toBeTruthy();
  });
});
